import 'package:flutter/material.dart';

class CashierPopularProducts extends StatelessWidget {
  const CashierPopularProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cashier Popular Products'),
    );
  }
}
