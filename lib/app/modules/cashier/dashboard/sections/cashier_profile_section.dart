import 'package:flutter/material.dart';

class CashierProfileSection extends StatelessWidget {
  const CashierProfileSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cashier Profile Section'),
    );
  }
}
