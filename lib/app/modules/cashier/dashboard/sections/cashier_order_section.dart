import 'package:flutter/material.dart';

class CashierOrderSection extends StatelessWidget {
  const CashierOrderSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cashier Order Section'),
    );
  }
}
