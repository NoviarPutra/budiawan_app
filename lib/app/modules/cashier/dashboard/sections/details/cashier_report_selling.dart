import 'package:flutter/material.dart';

class CashierReportSelling extends StatelessWidget {
  const CashierReportSelling({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cashier Report Selling'),
    );
  }
}
